# Fonts

`Roboto-Regular.ttf` is used for the game's interface.

- **Source:** Google Fonts (`fonts.gstatic.com`)
- **Licence:** Apache License 2.0 — full text in `LICENSE-Roboto.txt`
- **Copyright:** Google LLC

`Roboto-Regular SDF.asset` is the TextMeshPro font asset generated from it by
`ProjectBootstrap.TmpSetup`; it is a derived work and carries the same licence.

The Apache licence requires this notice and the licence text to travel with the
font, which is why both files are committed rather than gitignored.
